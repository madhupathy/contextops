# LangGraph Adapter

Callback-based adapter for capturing LangGraph execution traces.

## Usage

```bash
contextops adapter scaffold langgraph
```

Generates a Python callback handler that captures chain starts, tool calls, and chain completions, then sends the trace to ContextOps.

## Status

Planned for v0.3.
