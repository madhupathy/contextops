# Workflow Agent Benchmark

Tests for tool-using workflow agents.

## Test Categories

- **Tool sequencing** — Are tools called in the correct order?
- **Approvals** — Are sensitive operations gated properly?
- **Side effects** — Are unintended mutations avoided?
- **Completion accuracy** — Does the workflow achieve the goal?

## Usage

```bash
contextops benchmark run workflow-agent
```
