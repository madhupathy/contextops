# context-manifest

Context assembly specification for ContextOps.

Defines how to represent the full context assembly process:
- Which sources were considered and chosen
- Which were rejected and why
- Prompt budget allocation per source
- Memory injection decisions
- ACL filtering outcomes

Used by: API service, CLI manifest commands, debug UI.
